/**
 * PROVENANCE: extracted from
 * ~/code/raycast/extensions/universal-copy-link/src/clipboard.ts on 2026-09-04
 * by sai-mx3. Only the shared type is vendored — this extension saves links
 * rather than writing multi-format clipboard payloads.
 */
export interface LinkResult {
  title: string;
  url: string;
}
